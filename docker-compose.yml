services:
  portfolio:
    build: .
    image: portfolio-site:latest
    container_name: portfolio-site
    restart: unless-stopped
    ports:
      # host:container — change 8080 to whatever port you want on your server.
      - "8080:80"
